import { SectionLanding } from "@/components/SectionLanding";

export default function TransactionsPage() {
  return (
    <SectionLanding
      description="This section is ready to become the destination for activity history, confirmations, and settlement states linked from the homepage and account menu."
      eyebrow="Transactions"
      primaryHref="/"
      primaryLabel="Back to Dashboard"
      secondaryHref="/swap-flow"
      secondaryLabel="Open Swap Flow"
      title="Transaction History Placeholder"
    />
  );
}
